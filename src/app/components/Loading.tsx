export default function Loading({ isLoading }: { isLoading: boolean }) {
    return (
        <div className={`flex flex-col items-center justify-center h-screen bg-violet-950 text-white`} >
        </div>
    );
}